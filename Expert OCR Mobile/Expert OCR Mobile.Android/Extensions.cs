using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Expert_OCR_Mobile.Droid
{
    public class Extensions
    {
        static Context _context;
        /*
        public static DisplayAlert(string Title, string Message, string Button)
        {
            AlertDialog.Builder alertDialog = new AlertDialog.Builder(_context);
            alertDialog.SetTitle(Title);
            alertDialog.SetMessage(Message);

            alertDialog.SetPositiveButton(Button, delegate
            {
                alertDialog.Dispose();
                Toast.MakeText(_context, "OK pressed", ToastLength.Long).Show();

                //StartActivity(typeof(MainActivity));
            });
            alertDialog.Show();
        }*/
    }
}